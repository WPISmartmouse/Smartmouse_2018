package gzmaze.msgs;
import "pose.proto";

message RobotState
{
  required gazebo.msgs.Pose    position    = 1;
  required float               left_wheel_velocity  = 2;
  required float               right_wheel_velocity = 3;

}
