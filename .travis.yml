language: cpp
sudo: required
dist: trusty
compiler: gcc
before_script:
  - ./configure
  - mkdir build
  - cd build
  - cmake -Dtest=ON ..
script: make && make arduino && make test
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y arduino
