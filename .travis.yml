language: cpp
sudo: required
dist: trusty
compiler: gcc
before_script:
  - ./configure
  - mkdir build
  - cd build
  - cmake ..
script: make && make arduino
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y arduino
